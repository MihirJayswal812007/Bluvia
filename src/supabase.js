/**
 * BLUVIA — Supabase Client (REMOVED)
 * This file is no longer used. All data is served from db.js (localStorage).
 * Kept as a placeholder for future backend integration.
 */

// No longer needed — using local database (db.js)
export const supabase = null;
export default supabase;
